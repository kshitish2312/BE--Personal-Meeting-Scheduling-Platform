const slotBookingModel = require('./SlotBooking');
const userModel = require('./User');
const userAvailabilityModel = require('./UserAvailability');
const weekModel = require('./Week');

module.exports = {
  slotBookingModel,
  userModel,
  userAvailabilityModel,
  weekModel,
};
